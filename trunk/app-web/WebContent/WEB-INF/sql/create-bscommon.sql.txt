/** 
use bsframework;

Creación del menu en base bsframework 
INSERT INTO tOption(cKey, cLabel, cUrl, cParent) VALUES('COMMON', 'Tablas', NULL, NULL);
SET @menuMain = LAST_INSERT_ID();

INSERT INTO tOption(cKey, cLabel, cUrl, cParent) VALUES('BOARD', 'Tablon', '/servlet/admin/BoardManager', 1);
INSERT INTO tR_RolOption(cRol, cOption) VALUES(1, LAST_INSERT_ID());
INSERT INTO tOption(cKey, cLabel, cUrl, cParent) VALUES('PARAMS', 'Par&aacute;metros del Sistema', '/servlet/common/ParameterManager', 1);
SET @menuId = LAST_INSERT_ID();
INSERT INTO tR_RolOption(cRol, cOption) VALUES(1, @menuId);

INSERT INTO tOption(cKey, cLabel, cUrl, cParent, cType) VALUES('ADD_PARAMS', 'Bot&oacute;n Nuevo', null, @menuId, 2);
INSERT INTO tOption(cKey, cLabel, cUrl, cParent, cType) VALUES('MOD_PARAMS', 'Bot&oacute;n Modificar', null, @menuId, 2);
INSERT INTO tOption(cKey, cLabel, cUrl, cParent, cType) VALUES('DEL_PARAMS', 'Bot&oacute;n Borrar', null, @menuId, 2);

INSERT INTO tOption(cKey, cLabel, cUrl, cParent) VALUES('COMUNA', 'Comunas', '/servlet/admin/ComunaManager', @menuMain);

drop table if exists tComuna;
drop table if exists tBoard;
*/

use bscommon;

CREATE TABLE tComuna (
  `cId` BIGINT PRIMARY KEY UNIQUE AUTO_INCREMENT,
  `cName` varchar(50) NOT NULL,
  `cRegion` Integer NOT NULL,
  `cPrioridad` Integer NOT NULL DEFAULT '0'
) ENGINE=InnoDB;

CREATE TABLE `tBoard` (
  `cId` BIGINT PRIMARY KEY UNIQUE AUTO_INCREMENT,
  `cType` varchar(15) NOT NULL,
  `cKey` varchar(10) DEFAULT '',
  `cValue` Varchar(200) NOT NULL,
  cEnable BIT NOT NULL DEFAULT 1
) ENGINE=InnoDB;

