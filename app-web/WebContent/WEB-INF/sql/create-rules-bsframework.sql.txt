use bsframework;

ALTER TABLE tR_RolOption ADD PRIMARY KEY(cRol, cOption);

ALTER TABLE tR_UserRol ADD PRIMARY KEY(cUser,cRol);

ALTER TABLE `bsframework`.`tParameter` 
ADD CONSTRAINT `withSystem` FOREIGN KEY (`cSystem` ) REFERENCES `bscommon`.`tBoard` (`cId`); 
